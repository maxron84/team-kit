# Backlog — T.E.A.M.-Starterkit

Aufgaben am **Kit selbst**, die keine eigene Kaskade rechtfertigen: kleine
Verbesserungen, technische Schulden, Rückmeldungen aus Feldprojekten.

> **Nicht verwechseln:** `bootstrap/backlog.md` ist die **Vorlage** für
> Zielprojekte. Diese Datei ist der Backlog des Kits.

**Nummernraum**: `BL-n` ist historisch gewachsen und wird zwischen Ursprungs-
projekt (`website-maxron-de`), Kit und Feldprojekten geteilt. `BL-1`…`BL-5`
tragen hier dieselbe Bedeutung wie im Feldprojekt
`team-kit_project_platformer`, damit die Spur lesbar bleibt. Neue kit-eigene
Funde ab `BL-6`. Verweise auf den Backlog eines **anderen** Projekts werden
`Kit-BL-<N>` geschrieben (`BL-50`).

> **Abgetragene Einträge stehen im Archiv:**
> [`backlog-archiv.md`](backlog-archiv.md). Dort liegt die vollständige
> Begründung jedes erledigten Punktes — sie wird nachgeschlagen, nicht
> mitgelesen. Diese Datei trägt nur, woran noch Arbeit hängt (`BL-53`).

**Stand 2026-08-20: fünf offene Einträge** (`BL-111`, `BL-112`, `BL-114`,
`BL-115`, `BL-116`). Die zuletzt abgetragenen (`BL-62`, `BL-108`, `BL-109`,
`BL-110`, `BL-113`) stehen mit voller Begründung im Archiv.

| Nr | Was | Woher | Status |
|---|---|---|---|
| BL-116 | **Ein Transkript, zwei Closeouts — der Abo-Messweg bucht beim zweiten Mal die Summe beider Kaskaden.** Wer zwei Kaskaden in **derselben** interaktiven Sitzung abschliesst, misst beim zweiten Closeout wieder das **ganze** Transkript; der bereits gebuchte Teil steckt darin und wandert ein zweites Mal ins Ledger. **Die vier Eigenschaften eines tauglichen Abo-Messwegs decken den Fall nicht** ([`doku/anhang-a.md`, A.9](../doku/anhang-a.md), aus `BL-33`): „Ein Transkript je Aufruf" verbietet das Summieren **mehrerer** Transkripte und sagt nichts über **ein** Transkript mit zwei Buchungspunkten; die Deduplikation über die Nachrichten-ID greift nicht, weil jede Antwort der ersten Hälfte genau einmal vorkommt — nur eben bereits bezahlt. Auch der A1-Kollisionsschutz greift nicht: Er schlägt bei **derselben Rolle + Kaskade** an, hier entstehen **zwei** Kaskadennummern und damit zwei unauffällige Zeilen. Das ist die **fünfte** Eigenschaft, die A.9 fehlt: Ein Messweg muss den **schon gebuchten Abschnitt** des Transkripts ausnehmen können. Dieselbe Bauart wie `BL-33`, `BL-45` und `BL-5`: Die Messung ist nicht falsch gerechnet, sie zählt nur einen Zeitraum zweimal — und fällt in **keiner** der bestehenden Prüfungen auf, weil beide Zeilen für sich plausibel sind | Feldprojekt `team-kit_project_platformer`, dortiges `BL-120` (Architekt-Closeout, Sofortmassnahme dort: Rohwert minus bereits gebucht, mit der Rechnung im Notiztext der Ledger-Zeile). **Der Eintrag stand dort auf „offen — zu melden", ohne dass hier je einer entstand** — aufgefallen beim Backlog-Durchgang 2026-08-20, der die Feldeinträge gegen den Kit-Backlog hielt. Zuständigkeitslage wie bei `BL-33`: Das Messwerkzeug (`~/.claude/scripts/architekt_kosten.py`) gehört dem Kit nicht, aber die A1-Regel des Kits verlässt sich auf einen tauglichen Messweg | **offen.** Fix-Richtung wie bei `BL-33` bereits entschieden — das fremde Skript **nicht** einziehen, sondern A.9 um die fünfte Eigenschaft ergänzen, mit dem Feldfall als Beleg, damit die Regel an einer prüfbaren Eigenschaft hängt statt an einem Dateinamen. **Zweite, billigere Hälfte:** Die Falle entsteht erst durch zwei Closeouts in **einer** Sitzung — die Architekten-Anweisung kann sie vermeiden statt korrigieren: ein Satz in [`team/prompts/rolle-architekt.md`](../team/prompts/rolle-architekt.md), dass nach einem gebuchten Closeout die nächste Kaskade eine **neue** Sitzung bekommt, sonst der bereits gebuchte Betrag ausdrücklich abzuziehen ist. **Beim Umsetzen mitzuführen:** A.9-Zeilen tragen den Träger `anhang-a` und werden von `kit-regelinventar.py` geprüft — eine neue Eigenschaft ohne Inventarzeile fällt dort auf |
| BL-114 | **`frank.sh` rollt mit `git reset --hard` + `git clean -fd` blank zurück — genau das, was `team/lib.sh` sich selbst ausdrücklich verbietet.** [`entry/frank.sh`](../entry/frank.sh) führt auf **zwei** Pfaden ein unbeschränktes `git reset --hard "$START_HASH"; git clean -fd` aus: beim Session-Limit (Exit 42) und beim Fehlversuch. [`team/lib.sh`](../team/lib.sh#L636-L648) beschreibt im Kopf des Read-Only-Guards **wörtlich die Gegenregel**: *„Bei Verletzung wird NUR jeder einzelne Verletzer-Pfad zurückgesetzt — niemals blanko `git reset --hard`/`clean -fd`. (Lektion 2026-07-10: ein blindes reset+clean löschte einmal die gesamte uncommittete Team-Infrastruktur. Nie wieder.)"* Die Lehre ist also am **Guard** angewandt worden und am **Aufrufer** nicht — `team_guard_verify` arbeitet chirurgisch mit `git checkout <hash> -- <pfad>` und hat seit `BL-16` sogar einen Schnappschuss des Ausgangszustands, damit fremde uncommittete Arbeit nicht angelastet wird; `frank.sh` wirft zwei Zeilen weiter alles weg, was der Guard gerade sorgfältig geschont hat. **Betroffen ist jede uncommittete Arbeit im Zielprojekt**, nicht nur die der Rolle: eine parallele Sitzung, eine Handänderung, eine noch nicht committete Closeout-Ausgabe des Architekten. **Nachtrag 2026-08-20 (Nachprüfung beim Backlog-Durchgang): Der Befund ist breiter als sein Titel.** **(a) Der Windows-Zweig ist identisch betroffen** und stand hier nicht: [`entry/frank.ps1`](../entry/frank.ps1#L118) führt auf denselben zwei Pfaden (Zeile 118 und 174) dasselbe unbeschränkte `git reset --hard $startHash` + `git clean -fd` aus. Ein Fix nur im Bash-Zweig repariert die Hälfte und erzeugt genau die Drift, vor der `BL-112` warnt. **(b) Die halbe Dosis steckt in zwei weiteren Rollen, je beidzweigig:** [`axel.sh:103`](../entry/axel.sh#L103), `axel.ps1`, [`redteam.sh:180`](../team/redteam.sh#L180) und `redteam.ps1` beschränken ihren `git clean -fd` auf Test-/Plan-Ordner — ihr `git reset --hard` ist aber ebenso **pfadlos** und verwirft jede uncommittete Änderung an verfolgten Dateien im ganzen Baum. Die Einschränkung, die dort schon jemand für nötig hielt, fehlt ausgerechnet am gefährlicheren der beiden Befehle. (`ralph.sh`, `harry.sh` und `marv.sh` haben keinen Rollback-Pfad und sind nicht betroffen.) **(c) Zeigerfehler an genau diesen Stellen** (`BL-50`-Bauart, im Vorbeigehen mitzunehmen): Der Kommentar *„Analog zu frank.sh (Zeile 68)"* in [`axel.sh:100`](../entry/axel.sh#L100) und [`redteam.sh:178`](../team/redteam.sh#L178) zeigt ins Leere — Zeile 68 trägt heute die BL-52-Prosa zum Auftragsumfang, gemeint ist der Exit-42-Rollback bei `frank.sh:112` | Feldprojekt `team-kit_project_platformer`, Architekt-Closeout K39, 2026-08-19. **Anlass war ein Verlustfall, dessen Mechanismus NICHT bewiesen ist** und deshalb nicht als Beleg zählt: Nach einem **erfolgreichen** Frank-Lauf (Versuch 1/3, kein Rollback-Pfad) fehlte von sieben uncommitteten Closeout-Dateien genau eine — `CHANGELOG.md`, also die Datei, in die Frank per Dreisatz selbst schreibt. Sechs andere waren unversehrt. Der Befund oben steht unabhängig davon: Er ist am Quelltext ablesbar | **offen.** Fix-Richtung: denselben chirurgischen Weg nehmen, den `team_guard_verify` schon geht — nur die Pfade zurücksetzen, die **diese Rolle in diesem Versuch** angefasst hat (der Guard kennt sie bereits), statt des Holzhammers. Der `git clean -fd` ohne Pfadeinschränkung hat einen dokumentierten Grund (`HM-29`: Frank läuft mit `bypassPermissions` und legt auch ausserhalb des Produktivordners Dateien an) — der bleibt berechtigt, braucht aber eine Pfadliste statt des ganzen Baums. **Gegenprobe, die den Fix erst gültig macht:** Ein Test, der vor dem Rollenlauf eine unbeteiligte uncommittete Datei anlegt und nachweist, dass sie den erzwungenen Fehlversuch überlebt. **Umfang nach der Nachprüfung:** beide Zweige (`.sh` **und** `.ps1`) und alle drei Rollen mit Rollback-Pfad (`frank`, `axel`, `redteam`); die Gegenprobe gehört deshalb in die **Doppelbahn** (`kit-test.sh`/`kit-test.ps1`), nicht nur in die Bash-Suite — sonst beweist der Test die Zusicherung für die Hälfte der Installationen |
| BL-112 | **Die Briefings sind geteilt, der ZUSAMMENGESETZTE Prompt ist es nicht — rund 140 Zeilen agentensteuernde Prosa liegen jetzt doppelt.** Der Plan [`windows-nativ.md`](windows-nativ.md) stützt sich auf die Feststellung, die driftgefährlichste Fläche sei „konstruktionsbedingt bereits single-source": [`team_briefing`](../team/lib.sh#L621) ist ein `cat` auf [`team/prompts/rolle-*.md`](../team/prompts/), und die 340 Zeilen Briefing gelten damit für beide Zweige. **Das stimmt — und es ist die halbe Wahrheit.** Der Prompt, der die Rolle wirklich erreicht, entsteht erst im Einstiegsskript: In [`team/redteam.sh`](../team/redteam.sh#L117-L152) sind das 35 Zeilen (Auftrag, `SCOPE_LINE`, `KONTROLLFLUSS_ZEILE` aus BL-39, die EISERNE REGEL, der Beutebuch-Block, der BL-47-Absatz), dazu je ~20 Zeilen in [`ralph.sh`](../entry/ralph.sh#L72-L84), [`frank.sh`](../entry/frank.sh#L78-L99) und [`axel.sh`](../entry/axel.sh#L54-L74). Diese Prosa steht seit Stufe 4 **zweimal** im Repo — einmal in `.sh`, einmal in `.ps1`. Wer eine Feldlehre in Harrys Auftrag nachschärft und nur eine Fassung anfasst, bekommt zwei Zweige, die **verschiedene Agenten** steuern. Kein Test schlägt an: Beide Zweige laufen grün, die Prompts sind nur nicht mehr dieselben. Das ist die einzige verbliebene Stelle, an der Drift **unsichtbar** wäre — überall sonst greift die Doppelbahn. **Nachgemessen 2026-08-20: noch keine Drift eingetreten.** Die vier Prompt-Blöcke wurden normalisiert verglichen (Variablensyntax herausgerechnet, `PROMPT="…"` gegen `@"…"@`) — `ralph`, `frank`, `axel` und `redteam` sind heute **zeichengleich**. Der Eintrag beschreibt also ein Risiko, keinen Schaden; das ist der günstigste Zeitpunkt für den Test, weil er grün startet und jede spätere Abweichung ihm zuzuschreiben ist. **Die Fläche ist grösser als oben angegeben:** Agentensteuernde Prosa liegt nicht nur in den vier Einstiegsskripten, sondern auch in der Bibliothek selbst — `SMOKE_ZEILE` in [`lib.sh:34`](../team/lib.sh#L34) und [`lib.psm1:144`](../team/lib.psm1#L144) trägt die Vordergrund-Regel aus `BL-41` und ist ebenfalls doppelt geführt | Kit, 2026-08-17 — bei der Portierung der Rollen ([plans/windows-nativ.md](windows-nativ.md), Stufe 4) aufgefallen und dort im Baustand vermerkt | **offen.** **Fix-Skizze, bewusst NICHT „Prosa nach `team/prompts/` auslagern":** Das wäre der naheliegende Weg und würde den Bash-Zweig verhaltensrelevant anfassen — was die Stufen 1–4 ausdrücklich nicht tun. Stattdessen ein **Gleichstands-Test in der Doppelbahn**, dieselbe Bauart wie Schritt 10/10 in `kit-test.sh`: ein `claude`-Stub, der sein `-p`-Argument in eine Datei schreibt statt zu arbeiten, jede Rolle einmal je Bahn gefahren, und die beiden Prompt-Dateien **zeichenweise** verglichen. Das prüft nicht, woran jemand gedacht hat, sondern jede Zeile, die auseinanderläuft. Der Stub gehört in `Schale.claude_stub` (existiert bereits, muss nur die Argumente mitschreiben). Danach ist auch die Auslagerung nach `team/prompts/` gefahrlos möglich — mit einem Test, der beweist, dass sie den Prompt nicht verändert hat. **Eine Stolperstelle der Skizze, vor dem Bau zu klären:** Der zeichenweise Vergleich schlägt auch auf **legitime** Zweigunterschiede an — `SMOKE_ZEILE` nennt im Bash-Zweig `team.config.sh` und im PowerShell-Zweig `team.config.ps1`, und das ist richtig so. Der Test braucht deshalb eine **ausgewiesene Ausnahmeliste mit Begründung je Eintrag**; ohne sie ist er am ersten Tag rot und wird abgeschaltet statt gelesen, und ohne die Begründungspflicht wird die Liste zur Sammelstelle, hinter der die echte Drift verschwindet |
